from tkinter import *

#GUI Base built by creating an instance of Tk class
priyansh_root = Tk()

#GUI LOGIC is to be written in the event loop
#(i.e. mainloop), which keeps user in the GUI
#for futher use of the Application

priyansh_root.mainloop()
